diff --git a/node_modules/expo-notifications/android/src/main/java/expo/modules/notifications/notifications/presentation/builders/ExpoNotificationBuilder.kt b/node_modules/expo-notifications/android/src/main/java/expo/modules/notifications/notifications/presentation/builders/ExpoNotificationBuilder.kt
index 610d303..efbb1d3 100644
--- a/node_modules/expo-notifications/android/src/main/java/expo/modules/notifications/notifications/presentation/builders/ExpoNotificationBuilder.kt
+++ b/node_modules/expo-notifications/android/src/main/java/expo/modules/notifications/notifications/presentation/builders/ExpoNotificationBuilder.kt
@@ -11,7 +11,9 @@ import android.os.Parcel
 import android.provider.Settings
 import android.util.Log
 import androidx.core.app.NotificationCompat
+import androidx.core.app.Person
 import androidx.core.app.RemoteInput
+import androidx.core.graphics.drawable.IconCompat
 import expo.modules.notifications.notifications.SoundResolver
 import expo.modules.notifications.notifications.enums.NotificationPriority
 import expo.modules.notifications.notifications.interfaces.INotificationContent
@@ -96,6 +98,10 @@ open class ExpoNotificationBuilder(
     builder.setPriority(priority)
 
     val content = notificationContent
+    val notificationImage = if (content.containsImage()) content.getImage(context) else null
+    val notificationType = content.body?.optString("type").orEmpty().uppercase()
+    val isChitthiNotification = notificationType == "CHITTHI_MESSAGE" ||
+      notificationType == "FCHAT_MESSAGE" || notificationType == "CHITTHI_REACTION"
 
     builder.setAutoCancel(content.isAutoDismiss)
     builder.setOngoing(content.isSticky)
@@ -104,9 +110,35 @@ open class ExpoNotificationBuilder(
     builder.setContentTitle(content.title)
     builder.setContentText(content.text)
     builder.setSubText(content.subText)
-    // Sets the text/contentText as the bigText to allow the notification to be expanded and the
-    // entire text to be viewed.
-    builder.setStyle(NotificationCompat.BigTextStyle().bigText(content.text))
+    if (isChitthiNotification) {
+      val senderName = content.body?.optString("senderName")?.takeIf { it.isNotBlank() }
+        ?: content.title?.takeIf { it.isNotBlank() }
+        ?: "FairFares member"
+      val senderId = content.body?.optString("senderId")?.takeIf { it.isNotBlank() }
+      val conversationName = content.body?.optString("conversationName").orEmpty().trim()
+      val isGroup = content.body?.optBoolean("isGroup", false) == true || conversationName.isNotBlank()
+      val renderedText = content.text.orEmpty()
+      val messageText = if (isGroup && conversationName.isNotBlank()) {
+        renderedText.removePrefix("$conversationName\n")
+      } else {
+        renderedText
+      }.ifBlank { "New Chitthi letter" }
+      val senderBuilder = Person.Builder().setName(senderName)
+      senderId?.let { senderBuilder.setKey("fairfares-user-$it") }
+      notificationImage?.let { senderBuilder.setIcon(IconCompat.createWithBitmap(it)) }
+      val style = NotificationCompat.MessagingStyle(
+        Person.Builder().setName("You").setKey("fairfares-current-user").build()
+      )
+        .setGroupConversation(isGroup)
+        .addMessage(messageText, notification.originDate.time, senderBuilder.build())
+      if (isGroup) style.setConversationTitle(conversationName.ifBlank { "Chitthi group" })
+      builder.setStyle(style)
+      builder.setCategory(NotificationCompat.CATEGORY_MESSAGE)
+    } else {
+      // Sets the text/contentText as the bigText to allow the notification to be expanded and the
+      // entire text to be viewed.
+      builder.setStyle(NotificationCompat.BigTextStyle().bigText(content.text))
+    }
 
     color?.let { builder.color = it.toInt() }
     notificationContent.badgeCount?.toInt()?.let { builder.setNumber(it) }
@@ -147,9 +179,8 @@ open class ExpoNotificationBuilder(
       )
     )
 
-    if (notificationContent.containsImage()) {
-      val bitmap = notificationContent.getImage(context)
-      bitmap?.let { builder.setLargeIcon(it) }
+    if (notificationImage != null) {
+      builder.setLargeIcon(notificationImage)
     } else {
       builder.setLargeIcon(largeIcon)
     }
